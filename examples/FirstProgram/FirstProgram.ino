#include <puppybot.h>    //ประกาศชุดคำสั่งสำหรับบอร์ด PuppyBot
                         //อย่าลืมประกาศทุกครั้ง หากไม่ประกาศโปรแกรมจะทำงานไม่ได้

//เราสามารถดูตัวอย่างคำสั่งได้จาก Tab ข้างๆ 



void setup() {
 puppybot_setup();       //เรียกใช้งานฟังก์ชั้นต่าง
  wait_SW1();
 // เขียนคำสั่งสำหรับการทำงาน เพียงครั้ง เดียวหลังจากบรรทัดนี้ 
}

void loop() {
  // เขียนคำสั่งสำหรับการทำงาน ซ้ำๆ หรือ วนลูป หลังจากบรรทัดนี้ 
}
